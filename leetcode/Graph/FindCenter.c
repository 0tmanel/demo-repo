#include <stdio.h>
int findCenter(int** edges, int edgesSize, int* edgesColSize) {
    
}
int main(void)
{
    int arr[3][2] = {{1, 2}, {2, 3}, {4, 2}};
    int edgsSize = 3;
    int edgesColSize;
    int sum = findCenter(arr, edgsSize, &edgesColSize);
}